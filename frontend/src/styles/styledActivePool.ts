import styled from "styled-components";
import {Grid} from "@mui/material";

export const PoolContainer = styled(Grid)`
  margin-top: 30px
`;

export const PoolDescriptionItem = styled(Grid)`
    margin-top: 10px;
`;

export const PoolDescription = styled(Grid)``

export const PurchaseContainer = styled(Grid)`
    margin-top: 30px;
`;
