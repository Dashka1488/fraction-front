import styled from "styled-components";
import {Grid} from "@mui/material";

export const PoolsFlexContainer = styled(Grid)`
    margin-top: 20px;
`
export const NFTContainer = styled(Grid)`
   justify-content: center;
`

